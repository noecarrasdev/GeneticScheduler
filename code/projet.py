#import neat
import numpy as np
from math import ceil
import json
'''
def main():
    liste_node= []
    with open('./ProjetST7/smallRandom.json', 'r') as data:
        data_read = data.read()
        json_data = json.loads(data_read)["nodes"]
        for k in range(1,11):
            
            liste_node.append(node(k,json_data[str(k)]["Data"],json_data[str(k)]["Dependencies"] ) )
    test = ordre(liste_node)
    # crée un premier ordre, 1,2,3...,10. C'est licite mais très particulier. Pratique pour des tests
    nodes = loadNodes('smallRandom.json')
    ordreTest = ordre(nodes)
    print(ordreTest.ordreToCPU())
'''
#time1 = time(0,0,0,0)
#time2 = time(1,2,3,4)

